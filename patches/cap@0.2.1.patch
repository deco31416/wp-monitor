diff --git a/src/binding.cc b/src/binding.cc
index 04f3d4b..a69938d 100644
--- a/src/binding.cc
+++ b/src/binding.cc
@@ -70,0 +71,2 @@ class Pcap : public Nan::ObjectWrap {
+    bool watcher_initialized;
+    bool referenced;
@@ -88,0 +91,2 @@ class Pcap : public Nan::ObjectWrap {
+      watcher_initialized = false;
+      referenced = false;
@@ -100 +104,3 @@ class Pcap : public Nan::ObjectWrap {
-      if (pcap_handle && !closing) {
+      if (pcap_handle && !closing) {
+        bool watcher_close_pending = false;
+        closing = true;
@@ -106 +112,4 @@ class Pcap : public Nan::ObjectWrap {
-        uv_close((uv_handle_t*)&async, cb_close);
+        if (watcher_initialized) {
+          uv_close((uv_handle_t*)&async, cb_close);
+          watcher_close_pending = true;
+        }
@@ -108 +117,5 @@ class Pcap : public Nan::ObjectWrap {
-        uv_poll_stop(&poll_handle);
+        if (watcher_initialized) {
+          uv_poll_stop(&poll_handle);
+          uv_close((uv_handle_t*)&poll_handle, cb_close);
+          watcher_close_pending = true;
+        }
@@ -110,2 +123,3 @@ class Pcap : public Nan::ObjectWrap {
-        closing = true;
-        cleanup();
+        watcher_initialized = false;
+        if (!watcher_close_pending)
+          cleanup();
@@ -123 +137,4 @@ class Pcap : public Nan::ObjectWrap {
-        Unref();
+        if (referenced) {
+          Unref();
+          referenced = false;
+        }
@@ -186,2 +202,14 @@ class Pcap : public Nan::ObjectWrap {
     static void cb_close(uv_handle_t* handle) {
+      Pcap *obj = (Pcap*)handle->data;
+      Nan::HandleScope scope;
+      Local<Value> emit_argv[1] = {
+        Nan::New<String>("close").ToLocalChecked()
+      };
+      obj->async_res.runInAsyncScope(
+        Nan::New<Object>(obj->persistent()),
+        Nan::New<Function>(obj->Emit),
+        1,
+        emit_argv
+      );
+      obj->cleanup();
     }
@@ -212,0 +228,14 @@ class Pcap : public Nan::ObjectWrap {
+    static void cb_close(uv_handle_t* handle) {
+      Pcap *obj = (Pcap*)handle->data;
+      Nan::HandleScope scope;
+      Local<Value> emit_argv[1] = {
+        Nan::New<String>("close").ToLocalChecked()
+      };
+      obj->async_res.runInAsyncScope(
+        Nan::New<Object>(obj->persistent()),
+        Nan::New<Function>(obj->Emit),
+        1,
+        emit_argv
+      );
+      obj->cleanup();
+    }
@@ -402,7 +430,13 @@ class Pcap : public Nan::ObjectWrap {
                         &obj->async,
                         (uv_async_cb)cb_packets);
-      assert(r == 0);
+      if (r != 0) {
+        obj->cleanup();
+        return Nan::ThrowError(uv_strerror(r));
+      }
+      obj->watcher_initialized = true;
       obj->async.data = obj;
+      obj->Ref();
+      obj->referenced = true;
       r = RegisterWaitForSingleObject(
         &obj->wait,
         pcap_getevent(obj->pcap_handle),
@@ -423,5 +461,10 @@ class Pcap : public Nan::ObjectWrap {
                       0,
                       nullptr);
+        Local<Value> error = Nan::Error(
+          errmsg ? errmsg : "RegisterWaitForSingleObject failed"
+        );
+        if (errmsg) LocalFree(errmsg);
+        obj->close();
-        return Nan::ThrowError(errmsg);
+        return Nan::ThrowError(error);
       }
 #else
@@ -429,7 +468,14 @@ class Pcap : public Nan::ObjectWrap {
-      assert(r == 0);
-      r = uv_poll_start(&obj->poll_handle, UV_READABLE, cb_packets);
-      assert(r == 0);
+      if (r != 0) {
+        obj->cleanup();
+        return Nan::ThrowError(uv_strerror(r));
+      }
+      obj->watcher_initialized = true;
       obj->poll_handle.data = obj;
+      obj->Ref();
+      obj->referenced = true;
+      r = uv_poll_start(&obj->poll_handle, UV_READABLE, cb_packets);
+      if (r != 0) {
+        obj->close();
+        return Nan::ThrowError(uv_strerror(r));
+      }
 #endif
-
-      obj->Ref();
